services:
  fetch:
    build: .
    container_name: fetch-ytdlp
    ports:
      - "4242:4242"
    volumes:
      - ./downloads:/app/downloads
      - ./cookies.txt:/app/cookies.txt
    restart: unless-stopped
